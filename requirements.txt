# Gateway runtime
fastapi>=0.115
uvicorn[standard]>=0.30
httpx>=0.27
pydantic>=2.7

# Native extension build
pybind11>=2.12
setuptools>=69

# Anomaly detection. scikit-learn is OPTIONAL at runtime: the scorer falls back to a
# robust z-score if it is missing, and below min_clients_for_forest that fallback is
# what runs anyway. Pinned here because CI must exercise both paths.
numpy>=1.26
scikit-learn>=1.4

# Tests
pytest>=8.0
